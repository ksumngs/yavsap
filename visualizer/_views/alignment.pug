extends layout

block layout-content
  h1 #{samplename}
  if !hascontigs
    div.alert.alert-danger
      i de novo
      |  assembly failed for this sample
  div#igviewer

block layout-scripts
  script(src="/js/igv/igv.min.js")
  script.
    igvDiv = $('#igviewer');
    options = !{JSON.stringify(options)};
    igv.createBrowser(igvDiv, options).
      then(function(browser) {
        igv.browser = browser;
      });
